<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Subject's Page</title>
  </head>
  <body>
    <h1>Subjects Data</h1>
    <h3><a href="/subject/add">add subject</a></h3>
    <table style="width:80%" border="1">
      <tr>
        <th>No.</th>
        <th>Subject Name</th>
        <th>Options</th>
      </tr>
      <% for(let i in subjectData) { %>
      <tr>
        <td><%= subjectData[i].id %></td>
        <td><%= subjectData[i].subject_name %></td>
        <td><a href=" /subject/edit/<%= subjectData[i].id %>">Edit</a> | <a href="/subject/delete/<%= subjectData[i].id %>" onclick="return confirm('Delete this subject?')"> Delete </a></td>
      </tr>
      <% } %>
    </table>
    <p><a href="/">Back</a></p>
  </body>
</html>
